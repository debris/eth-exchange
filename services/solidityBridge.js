var web3 = require('ethereum.js');
var clientRecipient = require('./clientRecipient'); 

/**
 * Should be called on app start, to bind all solidity Events (callbacks).
 */
var setup = function () {
    
};

module.exports = {
    setup: setup
};

